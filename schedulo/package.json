{
  "name": "schedulo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --workspace client run build",
    "dev": "scripts\\dev.cmd",
    "start": "npm --workspace server run start",
    "test": "npm --workspace client test -- --watch=false"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
